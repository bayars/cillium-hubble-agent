stages:
  - test
  - build
  - release

variables:
  PYTHON_VERSION: "3.11"
  HELM_VERSION: "3.14.0"
  API_IMAGE: ${CI_REGISTRY_IMAGE}
  SIDECAR_IMAGE: ${CI_REGISTRY_IMAGE}/sidecar
  COLLECTOR_IMAGE: ${CI_REGISTRY_IMAGE}/collector

# ---------------------------------------------------------------------------
# Test Stage
# ---------------------------------------------------------------------------

lint:
  stage: test
  image: python:${PYTHON_VERSION}-slim
  before_script:
    - pip install uv
    - uv sync --dev
  script:
    - uv run ruff check api/ sidecar/ tests/

test:
  stage: test
  image: python:${PYTHON_VERSION}-slim
  before_script:
    - pip install uv
    - uv sync --dev
  script:
    - uv run pytest tests/ -v --tb=short
      --ignore=tests/test_e2e.py
      --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml
    expire_in: 30 days

helm-lint:
  stage: test
  image:
    name: alpine/helm:${HELM_VERSION}
    entrypoint: [""]
  script:
    - helm lint helm/network-monitor
    - helm template test helm/network-monitor --namespace test
    - helm template test helm/network-monitor
      --namespace test
      --set collector.enabled=true
      --set collector.pollIntervalMs=1000

# ---------------------------------------------------------------------------
# Build Stage
# ---------------------------------------------------------------------------

.docker-base:
  stage: build
  image: docker:27
  services:
    - docker:27-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_MERGE_REQUEST_ID

build-api:
  extends: .docker-base
  script:
    - docker build -t ${API_IMAGE}:${CI_COMMIT_SHORT_SHA} .

build-sidecar:
  extends: .docker-base
  script:
    - docker build
      -t ${SIDECAR_IMAGE}:${CI_COMMIT_SHORT_SHA}
      -f sidecar/Dockerfile .

build-collector:
  extends: .docker-base
  script:
    - docker build
      -t ${COLLECTOR_IMAGE}:${CI_COMMIT_SHORT_SHA}
      -f sidecar/Dockerfile.collector .

# ---------------------------------------------------------------------------
# Release Stage (only on tags)
# ---------------------------------------------------------------------------

.release-docker:
  stage: release
  image: docker:27
  services:
    - docker:27-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - docker login -u ${CI_REGISTRY_USER} -p ${CI_REGISTRY_PASSWORD} ${CI_REGISTRY}
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/

push-api:
  extends: .release-docker
  script:
    - VERSION=${CI_COMMIT_TAG#v}
    - docker build
      -t ${API_IMAGE}:${VERSION}
      -t ${API_IMAGE}:${CI_COMMIT_SHORT_SHA}
      -t ${API_IMAGE}:latest
      .
    - docker push ${API_IMAGE}:${VERSION}
    - docker push ${API_IMAGE}:${CI_COMMIT_SHORT_SHA}
    - docker push ${API_IMAGE}:latest

push-sidecar:
  extends: .release-docker
  script:
    - VERSION=${CI_COMMIT_TAG#v}
    - docker build
      -t ${SIDECAR_IMAGE}:${VERSION}
      -t ${SIDECAR_IMAGE}:${CI_COMMIT_SHORT_SHA}
      -t ${SIDECAR_IMAGE}:latest
      -f sidecar/Dockerfile .
    - docker push ${SIDECAR_IMAGE}:${VERSION}
    - docker push ${SIDECAR_IMAGE}:${CI_COMMIT_SHORT_SHA}
    - docker push ${SIDECAR_IMAGE}:latest

push-collector:
  extends: .release-docker
  script:
    - VERSION=${CI_COMMIT_TAG#v}
    - docker build
      -t ${COLLECTOR_IMAGE}:${VERSION}
      -t ${COLLECTOR_IMAGE}:${CI_COMMIT_SHORT_SHA}
      -t ${COLLECTOR_IMAGE}:latest
      -f sidecar/Dockerfile.collector .
    - docker push ${COLLECTOR_IMAGE}:${VERSION}
    - docker push ${COLLECTOR_IMAGE}:${CI_COMMIT_SHORT_SHA}
    - docker push ${COLLECTOR_IMAGE}:latest

helm-push:
  stage: release
  image:
    name: alpine/helm:${HELM_VERSION}
    entrypoint: [""]
  script:
    - VERSION=${CI_COMMIT_TAG#v}
    - sed -i "s/^version:.*/version: ${VERSION}/" helm/network-monitor/Chart.yaml
    - sed -i "s/^appVersion:.*/appVersion: \"${VERSION}\"/" helm/network-monitor/Chart.yaml
    - helm package helm/network-monitor
    - helm registry login -u ${CI_REGISTRY_USER} -p ${CI_REGISTRY_PASSWORD} ${CI_REGISTRY}
    - helm push network-monitor-${VERSION}.tgz oci://${CI_REGISTRY}/${CI_PROJECT_NAMESPACE}/charts
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  needs:
    - push-api
    - push-sidecar
    - push-collector
    - helm-push
  script:
    - echo "Creating release for ${CI_COMMIT_TAG}"
  release:
    tag_name: ${CI_COMMIT_TAG}
    name: "Release ${CI_COMMIT_TAG}"
    description: "Release ${CI_COMMIT_TAG}"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
